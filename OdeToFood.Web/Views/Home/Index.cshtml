@model IEnumerable<OdeToFood.Data.Models.Restaurant>
@{
    ViewBag.Title = "Home Page";
}

<div class="jumbotron">
    <div>There are @Model.Count() restaurents available</div>
</div>

<ul>
    @foreach(var r in Model)
    {
        <li>@r.Name</li>
    }
</ul>